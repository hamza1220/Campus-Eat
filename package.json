{
  "name": "CEat",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.3.1",
    "concurrently": "^4.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-native": "^0.59.4",
    "react-navigation": "^3.6.1",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "node index.js",
    "build": "cd client/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not ie <= 11",
    "not op_mini all"
  ]
}
