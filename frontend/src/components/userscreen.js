import React, { Component } from 'react';

export default class Userscreen extends Component {
    render() {
        return (
            <div>
            	USER SCREEN HERE
            </div>
        );
    }
}